# PHP Registration Service
## prerequisites
Create `.env` file and populate it based on `.env.example` file.
## Usage
```bash
    cd docker
    docker-compose up -d --build
    docker exec -it registration-service-app bash
    composer install
```